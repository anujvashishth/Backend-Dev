import fs from "fs"
function createBooking(bookingid, Username, movies){
    let total=0;
    try{
        let user = [];
        let obj = {
            bookingid: new Date(), Username, movies     
    }
}export default createBooking