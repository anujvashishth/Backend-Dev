import jwt from "jsonwebtoken";
import bcrypt from "bcrypt";
import { StatusCodes } from "http-status-pro-js";

function productmid(req,res,next){
    try {
        
    } catch (error) {
        console.log(error)
        
    }
}